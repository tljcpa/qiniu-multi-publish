# Web 框架：async 原生，适配 LLM 流式输出
fastapi==0.115.5
# ASGI 服务器
uvicorn[standard]==0.32.1
# 配置读取（从环境变量 / .env 载入，带类型校验）
pydantic-settings==2.6.1
# LLM 调用：DeepSeek 走 OpenAI 兼容协议，复用官方 SDK
openai==1.57.4
# 测试
pytest==8.3.4
httpx==0.28.1
